// @flow

import React, { Component } from 'react'
import { Text } from 'react-native';

export default class NoDests extends React.Component< {||}> {
  render() {
    return (<Text>
              No Destinations
            </Text>);
  }
}
